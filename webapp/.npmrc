# @idos-network/kwil-infra declares viem@2.55.11 as a peerOptional; this app pins a newer
# viem for the CCTP leg. The versions are compatible, so accept the mismatch instead of
# letting npm silently drop kwil-infra/enclave from the install (which breaks the idOS SDK).
legacy-peer-deps=true
